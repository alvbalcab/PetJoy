"""Modelos del paquete `custom_admin`.

Aquí pueden definirse modelos auxiliares usados por el panel
administrativo personalizado (p. ej. logs, vistas de control, etc.).
Actualmente no contiene modelos específicos.
"""

from django.db import models

# Create your models here.
